export default function Ramadan(props){
    return (
        <div style={{ fontSize: ".9rem", cursor: "pointer", color:"#1a0dab", fontFamily: "Roboto,Arial,sans-serif;"}}>
        {props.title}
        </div>
    )
}